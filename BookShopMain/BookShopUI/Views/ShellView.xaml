<Window x:Class="BookShopUI.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
       xmlns:local="clr-namespace:BookShopUI.Views"
        mc:Ignorable="d" FontSize="18"
        Title="ShellView" Height="450" Width="800">
    <DockPanel>
        <Menu DockPanel.Dock="Top" FontSize="18">
            <MenuItem Header="Aplikacja" MinWidth="146" Width="146">
                <MenuItem x:Name="Exit" Header="Wyłącz aplikację" Width="auto" Margin="-30,0,-45,0"/>
                <MenuItem x:Name="PublishingHouse1" Header="Wydawnictwa" />
            </MenuItem>
            <MenuItem Header="Konto">
                <MenuItem x:Name="LoginScreen" Header="Zaloguj się"  Padding="0" Margin="0"/>
                <MenuItem x:Name="LogOut" Header="Wyloguj się"  Padding="0" Margin="0"/>
            </MenuItem>
        </Menu>
        <Grid>
            <ContentControl x:Name="ActiveItem" Margin="5" />
        </Grid>
    </DockPanel>
</Window>
