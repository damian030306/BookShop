<Window x:Class="BookShopUI.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
       xmlns:local="clr-namespace:BookShopUI.Views"
        mc:Ignorable="d" FontSize="18"
        Title="ShellView" Height="650" Width="800">
    <DockPanel>
        <Menu DockPanel.Dock="Top" FontSize="18">
            <MenuItem Header="Aplikacja" MinWidth="146" Width="146">
                <MenuItem x:Name="Exit" Header="Wyłącz aplikację" Width="auto" />
                <MenuItem x:Name="PublishingHouse1" Header="Wydawnictwa" />
                <MenuItem x:Name="Authors1" Header="Autorzy" />
                <MenuItem x:Name="Books1" Header="Książki" />
                <MenuItem x:Name="AuthorsAdd1" Header="Dodaj autora" />
                <MenuItem x:Name="BooksAdd1" Header="Dodaj książkę" />
            </MenuItem>
            <MenuItem Header="Konto">
                <MenuItem x:Name="LogIn" Header="Zaloguj się"  Padding="0" Margin="0"/>
                <MenuItem x:Name="LogOut" Header="Wyloguj się"  Padding="0" Margin="0"/>
            </MenuItem>
        </Menu>
        <Grid>
            <ContentControl x:Name="ActiveItem" Margin="5" />
        </Grid>
    </DockPanel>
</Window>
